# 检出一律用 LF —— 这一行是给 **Windows CI** 用的，不是风格偏好。
#
# GitHub 的 windows runner 默认 core.autocrlf=true，检出时会把所有文本文件换成
# CRLF。构建链里有若干脚本是拿"生成的文本"和"文件里的文本"逐字节比对的
# （scripts/gen-color-fallback.mjs 比回退块、各 verify-*.ts 用正则扫源码），
# 行尾一变就全体误报。v0.8.9 的 beta 与正式两条构建就是这么整条挂掉的
# （2026-09-11，`npm run build` 第一步报"回退块与 oklch 原值不一致"，
# 而颜色其实一个字节都没改）。
#
# 本仓库开发在 Linux、产物在 Windows 跑，没有任何文件需要 CRLF
# （无 .bat/.cmd/.ps1）。text=auto 仍会自动识别二进制（图标、字体），不会破坏它们。
#
# 注意：这份文件经 release.py 同步到公开仓 lbg1690786597-ui/filmweaver 的**根目录**，
# CI 检出的就是它——所以它必须待在 desktop/ 下，挪到仓库根就对 CI 失效了。
* text=auto eol=lf
